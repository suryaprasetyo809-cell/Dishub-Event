[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqli", "php82Extensions.gd", "nodejs", "git"]

[phases.install]
cmds = ["composer install --no-dev", "npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "apache2-foreground"
